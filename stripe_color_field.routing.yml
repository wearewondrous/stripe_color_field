stripe_color_field.settings:
  path: '/admin/config/content/stripe_color_field'
  defaults:
    _form: '\Drupal\stripe_color_field\Form\StripeColorsForm'
    _title: 'Settings for Stripe Colors'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
